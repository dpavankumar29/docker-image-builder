version: '3'

services:

  backend:
    build: ./backend
    ports:
      - "5001:5001"

  redis:
    image: redis
    ports:
      - "6380:6379"

  nginx:
    image: nginx
    ports:
      - "8085:80"